# AGENTS.md

## Cursor Cloud specific instructions

- This repository ("glyphet") is a freshly initialized project with only a `README.md`.
- There are no application services, dependencies, build steps, lint checks, or tests to run.
- When application code is added, update this section with relevant build/run/test commands and any non-obvious caveats.
